import { PersianDirective } from './persian.directive';

describe('PersianDirective', () => {
  it('should create an instance', () => {
    const directive = new PersianDirective();
    expect(directive).toBeTruthy();
  });
});
